
from setuptools import setup, find_packages

setup(
    name="pyromaniac",
    author="Borna Bešić",
    description="Reload modules and classes and live-patch objects during runtime",
    packages=find_packages(),
)

